{
  "name": "com-croftsoft-app-evolve",
  "version": "0.1.0",
  "author": "David Wallace Croft <david@croftsoft.com>",
  "description": "Evolve critter movements via food distribution",
  "scripts": {
    "build": "rimraf dist pkg && cross-env NODE_OPTIONS=--openssl-legacy-provider webpack",
    "start": "rimraf dist pkg && cross-env NODE_OPTIONS=--openssl-legacy-provider webpack-dev-server --open -d",
    "test": "cargo test && wasm-pack test --firefox --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.6.0",
    "copy-webpack-plugin": "^5.1.2",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.12",
    "webpack-dev-server": "^3.11.3"
  }
}
